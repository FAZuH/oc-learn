/**
 * Shim — moved to plugins/viz/src/common.ts
 * Kept for backwards compat (one release); new code should import from
 * "../plugins/viz/src/common.ts" (viz server) or the published package.
 */
export * from "../plugins/viz/src/common.ts"
